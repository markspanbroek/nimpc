# This file is only here to keep the Visual Studio Code plugin happy
# Nimble will look at the config.nims in the parent directory
switch("path", "$projectDir/../../src")
switch("path", "$projectDir/../helpers")
