name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - uses: jiro4989/setup-nim-action@v1.0.1
      with:
        nim-version: '1.0.4'
    - run: nimble install
    - run: nimble test
